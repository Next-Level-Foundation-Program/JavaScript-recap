const name = "Rana";
//   name = "Rakib";

let age = 19
  age = 23

const isAdult = true;
console.log(name, age, isAdult);
console.log(typeof(name), typeof(age), typeof(isAdult));

// Data types in JS
//  ssbbnnuo
// string, symbol, bigint, boolean, number, null, undefined, object